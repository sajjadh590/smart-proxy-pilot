{
  "name": "proxypilot-engine",
  "version": "1.0.0",
  "description": "ProxyPilot Engine — lightweight local native-messaging host that bridges advanced proxy protocols to a local SOCKS5 endpoint.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "proxypilot-engine": "src/index.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "install-host": "node install/install.js",
    "uninstall-host": "node install/install.js --uninstall"
  },
  "engines": {
    "node": ">=18"
  }
}
